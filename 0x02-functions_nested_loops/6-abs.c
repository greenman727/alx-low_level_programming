#include "main.h"

/**
 * _abs - compute the absolute of an integer
 * @i: the integer to be computed
 * Return: the absolute value
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}

